ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-5
CORS_ORIGINS=http://localhost:5173

# DATABASE_URL and DATA_DIR default to <repo>/data — only set these to override.
# DATABASE_URL=sqlite:////absolute/path/to/apply_engine.db
# DATA_DIR=/absolute/path/to/data
